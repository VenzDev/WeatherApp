import styled from "styled-components";

const Input = styled.input`
  border-radius: 10rem;
  padding: 1.5rem 1rem;
  margin: 1.5rem 0;
`;

const Button = styled.button`
  border-radius: 10rem;
`;

export { Input, Button };
